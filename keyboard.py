LAT_TO_RUS = {
    'q': 'й', 'w': 'ц', 'e': 'у', 'r': 'к', 't': 'е', 'y': 'н', 'u': 'г', 'i': 'ш', 'o': 'щ', 'p': 'з',
    '[': 'х', ']': 'ъ', 'a': 'ф', 's': 'ы', 'd': 'в', 'f': 'а', 'g': 'п', 'h': 'р', 'j': 'о', 'k': 'л',
    'l': 'д', ';': 'ж', "'": 'э', 'z': 'я', 'x': 'ч', 'c': 'с', 'v': 'м', 'b': 'и', 'n': 'т', 'm': 'ь',
    ',': 'б', '.': 'ю', '/': '.', '`': 'ё', '~': 'Ё', '!': '!', '@': '"', '#': '№', '$': ';', '%': '%',
    '^': ':', '&': '?', '*': '*', '(': '(', ')': ')', '-': '-', '_': '_', '+': '+', '=': '=', '\\': '\\'
}

def fix_keyboard(text: str) -> str:
    """Переводит текст из латиницы в русскую раскладку, если есть русские буквы."""
    result = []
    for ch in text:
        lower = ch.lower()
        if lower in LAT_TO_RUS:
            new = LAT_TO_RUS[lower]
            if ch.isupper():
                new = new.upper()
            result.append(new)
        else:
            result.append(ch)
    fixed = ''.join(result)
    # Если после преобразования появились русские буквы – возвращаем исправленное
    if any('а' <= c <= 'я' or c in 'ёЁ' for c in fixed):
        return fixed
    return text  # иначе оставляем как есть

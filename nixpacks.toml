[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = [
  "python -m venv .venv",
  ".venv/bin/pip install -r requirements.txt"
]

[start]
cmd = "export PATH=$(find /nix/store -maxdepth 3 -type d -name bin | grep ffmpeg | head -1):$PATH && .venv/bin/python bot.py"